# Deploy用の環境変数設定例
# このファイルを `.env.deploy` にコピーして使用してください
#
# 使用方法:
#   cp scripts/ci-cd/env.deploy.example .env.deploy
#   # .env.deploy を編集して実際の値を設定
#   source .env.deploy
#   bash scripts/ci-cd/deploy-steps.sh

# 必須: GCPプロジェクトID
PROJECT_ID=your-project-id

# 必須: Terraform stateバケット名（gs://プレフィックスなし）
TFSTATE_BUCKET=your-tfstate-bucket-name

# オプション: リージョン（デフォルト: asia-northeast1）
REGION=asia-northeast1

# オプション: BUILD_SHA（デフォルト: git rev-parse HEAD）
# BUILD_SHA=$(git rev-parse HEAD)
