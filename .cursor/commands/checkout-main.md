# checkout-main: main ブランチへの切り替え

このコマンドは、main ブランチに切り替えて最新の状態に更新します。

**重要: このコマンド実行中は、すべての応答を日本語で行ってください。**

## 実行内容

```bash
git checkout main
git pull
```

---

実行: チャット入力で `/checkout-main` を選択してください。
