以下の手順でコードレビューを実施してください。

**回答は必ず日本語で行ってください。**

---

## ステップ0: 差分取得

```bash
git diff main
```

---

## 対象外ファイル

以下のパスに含まれるファイルは**すべてのステップでレビュー対象外**とする。

- `apps/client/components/ui/**` — shadcn/ui CLI で生成されたコード
- `apps/client/components/ui-shadcn/**`
- `**/shared/ui-shadcn/**`

---

## Step 1: 変化を把握する（必須・最初に実行）

### 1-A: 目的を 1 文で言語化する

この変更が何を達成しようとしているかを 1 文で述べる。

### 1-B: 振る舞いの変化をリストアップする

ファイル単位ではなく**振る舞い単位**で、以下の 2 軸を別々に列挙する。

**削除・変更された振る舞い:**

> 例: 「`AsyncBoundary` が削除され、エラーハンドリングが失われた」「認証チェックが `middleware` から各 handler へ移動した」

**新たに追加された振る舞い:**

> 例: 「ユーザーがメールアドレスを変更できるようになった」「レスポンスに `createdAt` フィールドが追加された」

### 1-C: 削除・変更された振る舞いを分類する

1-B の「削除・変更」リストの各項目に以下のいずれかのラベルを付ける：

- `[意図的削除]` — 目的に照らして削除が合理的であり、代替手段がある
- `[移動/改名]` — 振る舞い自体は残っているが、コードの場所・名前が変わった（旧コードが扱っていた全ケースを新コードが引き継いでいるか要確認）
- `[考慮漏れ候補]` — 削除・変更によって何かが壊れる可能性がある

> `[考慮漏れ候補]` と `[移動/改名]` が **Step 2 トラック A** の指摘候補となる。

### 1-D: 追加された振る舞いの期待動作を言語化する

1-B の「追加」リストの各項目について、以下を **MECE に** 列挙する：

- 正常系: どんな入力・操作に対して何を返すか
- 異常系: 起こりうるエラー・例外ケースをすべて列挙し、それぞれ何を返すべきか（例: 存在しないリソース→404、重複→409、権限なし→403、バリデーション失敗→400）

> **列挙が浅い場合（「エラー時は 400 を返す」等の抽象的な記述にとどまる場合）は、その振る舞いについて Track B を実行しないこと。**

> `[追加された振る舞い]` が **Step 2 トラック B** の検証対象となる。

---

## Step 2: 問題の証明（メインレビュー）

### トラック A: リグレッション検証

Step 1-C で `[考慮漏れ候補]` または `[移動/改名]` とした項目を起点に「実際に困る場面があるか？」を問い続ける。

**指摘を確定する 3 要件（すべて満たす場合のみ指摘する）:**

1. 失敗するコードパスを 1 本追跡できる
2. その失敗が変更によって**新たに発生した**ことを示せる
3. 変更前のコードがその問題を**防いでいた**ことを示せる（`[移動/改名]` の場合: 旧コードが扱っていたケースを新コードが扱えていないことを示せる）

### トラック B: 新機能正確性検証

Step 1-D で言語化した各追加振る舞いについて、実装が期待動作を満たしているかを検証する。

**指摘を確定する 3 要件（すべて満たす場合のみ指摘する）:**

1. Step 1-D で言語化した期待動作と実装が乖離しているコードパスを追跡できる
2. そのエラーパスまたはエッジケースが未実装・誤実装であることを示せる（例: バリデーション不足、エラーが `Ok` として返される、権限チェック漏れ）
3. テストにそのケースが存在しないか、assert が意味のある値を検証していないことを示せる

> **🟠 の注意**: 並行実行バグ・トランザクション境界の欠如など「コードパス 1 本では表現できない」カテゴリは、「失敗条件を再現可能なシナリオとして記述できる」ことをもって要件 1 の代替とする。

---

## Step 3: プロジェクト固有の落とし穴チェック（Step 2 の後に実行）

**このステップはチェックリストではない。** 変更が存在する項目のみ確認すること。該当しない項目はスキップしてよい。「問題なし」の確認レポートは出力しない。

各チェック項目で問題を見つけた場合は、**違反の内容と実際に引き起こす実害**をセットで記述すること。

### 3-A: アーキテクチャ依存方向

依存方向: `presentation → application → domain ← infrastructure → integrations`

- `domain/` 配下に Zod・Prisma・HTTP の import がないか
- DTOやバリデーション定義（`XxxInput`）が `application/validators.ts` にあるか
- 外部SDK（GCP等）が `src/integrations/` 以外で直接 import されていないか
- `features/` 配下で `process.env` を直接参照していないか（`src/config.ts` 経由であるべき）

### 3-B: ROP エラー型チェーン

- `usecase.ts` の先頭でエラー型が定義されているか
- 各 step のエラー型が usecase のエラー型と整合しているか（暗黙の型変換・潰しがないか）
- `presentation` 層（`router.ts`）で全 error ケースに対する HTTP ステータス分岐があるか（switch / if-else の網羅性）
- `andThen` / `asyncAndThen` / `map` の使い分けが正しいか（非同期ステップに `andThen` を使っていないか）

### 3-C: Value Object ライフサイクル

- 外部入力に対して `reconstitute〜` を使っていないか（バリデーションスキップになる）
- Application 層と Domain 層で同じバリデーションを二重に行っていないか

### 3-D: DB 操作の安全性

- 複数の DB 操作が単一のユースケース内に存在する場合、`prisma.$transaction` で囲まれているか
- リスト取得でループ内に DB クエリがないか（N+1 問題）。`findMany` の後にループで `findUnique` を呼ぶパターンは `include` または `IN` クエリに置き換えるべき

### 3-E: TypeScript

- `as` 型アサーションの不適切な使用がないか（`as const` / テスト内 `as unknown` / 型ファイルの `as never` は許容）
- `any` 型を使用していないか
- Zod v4 API を使用しているか（`z.email()`, `z.url()` を使用、`z.string().email()` は旧API）

### 3-F: テストカバレッジと品質

- 新機能・修正に対応するテストが追加されているか
  - api-service の変更には `usecase.test.ts` が存在するか
  - client の変更には `actions/*.test.ts` / `queries/*.test.ts` が存在するか
- テストの assert が意味のある値を検証しているか（`toBeTruthy()` のみ、空の期待値等は不十分）
- エラーパス・異常系のテストケースが含まれているか

---

## Step 4: セキュリティ（独立して確認）

- 認証チェックが presentation 層にあるか
- 入力バリデーションが `application/validators.ts` にあるか
- 変更前にあった認証・認可チェックが削除されていないか
- SQLインジェクション、XSS 等の OWASP Top 10

---

## 出力フォーマット

先頭に必ず Step 1 の結果を掲載する：

```
## 変更の意図と変化概要
- **目的**: （1文）
- **削除・変更された振る舞い**:
  - [考慮漏れ候補] ...
  - [移動/改名] ...（確認: 〇〇のケースが引き継がれているか）
  - [意図的削除] ...（代替: 〇〇で処理）
- **追加された振る舞い**:
  - [追加] ...（正常系: ... / 異常系: ...）
```

**重要度の区分:**

- 🔴 確実に壊れる / セキュリティホール
- 🟠 特定条件下で壊れる / エラーが握りつぶされる / 新機能の異常系が未実装
- 🟡 ルール違反だが即時障害にはならない

**🔴/🟠 の指摘フォーマット（証拠を必ず含める）:**

```
🔴 [ファイル名:行番号] タイトル

**種別**: リグレッション / 新機能バグ
**失敗シナリオ**: どのような操作・入力・状態のとき
**コードパス**: ファイル名:行番号 の 関数名 → ファイル名:行番号 の 関数名 → ここで X が起きる
**根拠**:
  - リグレッションの場合: 変更前のコードはなぜこの問題を防いでいたか
  - 新機能バグの場合: Step 1-D で言語化した期待動作と実装がどう乖離しているか
**Impact**: ユーザー・システムへの影響
**修正案**: 具体的な修正方法（コードスニペット推奨）
```

**🟡 の指摘フォーマット:**

```
🟡 [ファイル名:行番号] タイトル
**理由**: ルール違反の内容と実害
**修正案**: どう直すべきか
```

問題がない場合は「レビューOK」と一言添えてください。